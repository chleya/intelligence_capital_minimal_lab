你现在接手项目：

F:\intelligence_capital_minimal_lab

项目主线已经从 AEP-control 失败，演化为 Intelligence Capital Theory / 智能资本论，再进一步修正为 Intelligence Capital Portfolio Theory / 智能资本组合论。

核心认识：

1. AEP / Residual / Memory / Prototype / PolicyClone / Probe 都不是“智能本身”，而是一阶智能资本。
2. 真正的下一步不是证明某个资本永远最强，而是设计二阶智能：Capital Allocator。
3. 二阶智能不是 if-else 专家系统，而是基于资本绩效报表的闭环控制器。
4. Capital Allocator 不看世界是什么，只看每种资本在当前世界里兑现得怎么样。
5. IC-3 不能重犯传统特征工程错误，禁止使用 env_name、state_dim、utility_type 等手工世界特征调度。
6. IC-3 必须赢过 BestSingleCapital，否则二阶智能 claim 失败。
7. 所有正结果必须标注 SYNTH-ONLY 或 EXTERNALLY VALIDATED。
8. 钱学森工程控制论给本阶段的关键启发是：Allocator 必须是 feedback controller of intelligence capital，受稳定性、可观测性、可控性、鲁棒性、fallback 约束。
9. mHC / Manifold-Constrained Hyper-Connections 给本阶段的关键启发是：复杂连接必须有守恒约束。Capital weights 必须在 simplex 上，capital transition matrix 可约束在 Birkhoff polytope 上，避免权重爆炸、震荡和坏账放大。

现在执行：

IC-3-0 + IC-3A-F + IC-3B 合并阶段：
Performance-Reported, Manifold-Constrained, Cybernetic Capital Allocator

目标：
构建并审计一个最小二阶智能分配器。它只能读取各一阶资本的统一绩效报表，动态分配资本权重，并在扰动、目标变化、资本失效和外部任务中，长期 cumulative regret 低于 BestSingleCapital。

一、统一 CapitalReport 接口

新增或修正：
src/capital_report.py

定义 CapitalReport，所有资本必须输出同一格式：

- capital_name
- recommended_action
- predicted_utility
- confidence
- calibration_error
- recent_prediction_error
- recent_regret
- recent_realization_rate
- realized_utility_history
- capital_local_ood_score
- nearest_support_distance
- inference_cost
- update_cost
- storage_cost
- probe_cost
- goal_shift_score
- transfer_success_rate
- recent_transfer_regret
- expected_probe_value
- uncertainty_reduction_if_probe
- capital_age
- depreciation_score
- bad_debt_score
- impairment_flag

所有资本必须实现：

generate_report(context, history) -> CapitalReport
act(context, history) -> action
update(feedback) -> None

禁止 CapitalReport 或 Allocator 输入出现：

- env_name
- env_id
- state_dim
- utility_type
- mode_type
- friction
- delay_strength
- action_effect_rule_name
- hand_written_regime_label
- manually_computed_global_coverage

允许 coverage / OOD，但必须来自资本自身，例如 Memory 的 nearest neighbor distance、Prototype 的 assignment confidence、AEP 的 own OOD score。

二、实现最小资本集合

实现或整理以下资本：

1. PolicyCloneCapital
固定目标下强，输入 history，输出 action。

2. PrototypeOutcomeCapital
保存/查询 prototype outcome table，适合低维密集覆盖。

3. AEPCapital
学习 h,a -> outcome，适合 goal transfer。

4. ResidualCapital
学习 noop baseline + action residual，输出 action-effect estimate。

5. GoalInferenceCapital
用于 hidden-goal 或 goal-transfer 任务，但必须通过 CapitalReport 交互。

6. SafeFallbackCapital
所有资本 impaired 时使用，低收益但低风险。

三、Allocator 实现

新增：
src/capital_allocator.py
src/manifold_capital_allocator.py
src/cybernetic_allocator.py

实现以下 allocator：

1. BestSingleCapital
整个 test/eval split 上固定选择表现最好的单一资本。禁止 per-task / per-episode hindsight switching。

2. UniformPortfolio
等权组合。

3. RandomAllocator
随机选择资本。

4. OracleHindsightAllocator
每一步事后知道最优资本，仅作理论上限，不要求超过。

5. MetaMLPAllocator
输入所有 CapitalReport 数值字段，输出 capital_weights。

6. SimplexWeightAllocator
输出非负权重 w，满足：
w_i >= 0
sum_i w_i = 1

7. BirkhoffTransitionAllocator
输出 capital transition matrix M，表示上一时刻信任如何转移到下一时刻。
要求：
M_ij >= 0
row_sum(M)=1
col_sum(M)=1
用 Sinkhorn projection 实现。

8. FeedbackControlledAllocator
按工程控制论设计：
根据 tracking_error、recent_regret、confidence calibration、cost、ood_score 更新 capital reliability 和 weights。
要求：
- weights nonnegative
- sum(weights)=1
- max_weight_change_per_step <= delta
- impaired capital forced down
- fallback if all impaired
- confidence must decay if not recently validated

四、负迁移保护

必须实现：

1. Capital Impairment Detection
每个资本维护 recent_regret window。如果 recent_regret 连续 K 步高于 random baseline，标记 impaired，并强制降权或冻结。

2. Fallback Mechanism
如果所有资本 impaired，使用 SafeFallbackCapital 或 conservative safe policy。禁止所有资本都坏时继续高风险选择。

3. Depreciation Schedule
每个资本维护 last_validated_timestamp。长时间未验证或检测到 drift 时，confidence 自动衰减，衰减率不得为 0。

五、Feature Ban 审计

新增：
src/ic3_feature_ban.py

实现 audit_allocator_inputs(input_schema)。

输出：
results/ic3/allocator_input_schema.csv

若发现 env_name、state_dim、utility_type、regime_label 或其代理字段，立即 verdict：
IC3_FEATURE_ENGINEERING_REGRESSION

六、任务与环境

至少包含：

A. SYNTH fixed-goal task
PolicyClone 预期强。

B. SYNTH goal-transfer task
AEP / Residual 预期强。

C. SYNTH low-dim dense-support task
Prototype / Memory 预期强。

D. hidden-goal / partial-observable task
GoalInference / Probe 可能有价值。

E. 至少一个外部或半真实基准
可选：
- MiniGrid-like hidden-goal/gridworld
- simple robot navigation / pushing toy physics
- construction-site scheduling toy benchmark

所有结果必须标注：
SYNTH-ONLY 或 EXTERNALLY VALIDATED

如果没有外部任务，不允许宣称 general second-order intelligence。

七、工程控制论扰动测试

新增 disturbance tests：

1. sudden goal shift
2. gradual environment drift
3. one capital failure
4. memory aging
5. AEP extrapolation failure
6. probe cost spike
7. hidden-goal switch

记录：

- tracking_error
- regret_dynamics
- recovery_time_after_shift
- max_regret_spike
- weight_oscillation
- weight_turnover_rate
- stability_margin
- observability_score
- controllability_score
- impairment_detection_delay
- fallback_success_rate

八、mHC 风格稳定性审计

对 Simplex / Birkhoff allocator 输出：

- weight_entropy
- max_weight
- transition_norm
- row_sum_error
- col_sum_error
- capital_trust_explosion_score
- weight_turnover_rate
- bad_capital_amplification_score

死亡条件：

D_m1: weight collapse，单一资本长期权重 > 0.95 且 regret 上升
D_m2: row/col sum error > tolerance
D_m3: weight turnover 过高，说明 allocator 震荡
D_m4: unconstrained allocator 分数略高但稳定性显著更差
D_m5: constrained allocator 不能超过 BestSingleCapital

九、主指标

主指标只能是：

1. cumulative_regret
2. cost_normalized_cumulative_regret
3. realized_utility
4. cost_normalized_realized_utility
5. allocator_vs_best_single
6. oracle_gap = OracleHindsight - Allocator

其他指标只能作为 diagnostics，不能替代主指标。

最低成功标准：

Allocator cumulative regret < BestSingleCapital cumulative regret

并且必须满足：

- cost-normalized 后仍成立
- 至少跨 fixed-goal 与 goal-transfer 两类任务成立
- held-out / external split 上不崩
- 无禁用特征输入
- negative transfer protection 起作用

强成功标准：

Allocator > BestSingleCapital + 0.10
且 external validation 通过
且 seed 稳定
且 oracle_gap 随训练下降

十、必须输出的审计文件

results/ic3/capital_reports.csv
results/ic3/allocator_performance.csv
results/ic3/capital_weight_traces.csv
results/ic3/regret_curves.csv
results/ic3/cost_normalized_regret.csv
results/ic3/external_validation_detail.csv
results/ic3/feature_ban_audit.csv
results/ic3/best_single_definition_audit.csv
results/ic3/seed_stability.csv
results/ic3/negative_transfer_audit.csv
results/ic3/manifold_stability_audit.csv
results/ic3/cybernetic_control_diagnostics.csv
results/ic3/death_conditions.csv
results/ic3/taxonomy_stress_test.md

图：

results/figures/ic3_weight_trace.png
results/figures/ic3_regret_curve.png
results/figures/ic3_allocator_vs_single.png
results/figures/ic3_cost_normalized.png
results/figures/ic3_bad_capital_avoidance.png
results/figures/ic3_stability_diagnostics.png
results/figures/ic3_oracle_gap.png

十一、最终报告

生成：

IC3_FINAL_PERFORMANCE_REPORTED_CYBERNETIC_ALLOCATOR_REPORT.md

报告必须回答：

1. Allocator 是否真的超过 BestSingleCapital？
2. 是否 cost-normalized 后仍超过？
3. 是否超过 UniformPortfolio 与 RandomAllocator？
4. 是否没有读取禁用世界特征？
5. BestSingleCapital 是否定义正确？
6. 结果是否跨 seed 稳定？
7. 外部基准是否通过？若无，只能标注 SYNTH-ONLY。
8. 负迁移保护是否有效？
9. impairment 后权重是否及时下降？
10. fallback 是否能在全资本失效时触发？
11. Simplex / Birkhoff 约束是否减少震荡和坏账放大？
12. FeedbackControlledAllocator 是否比普通 MetaMLP 更稳定？
13. OracleHindsight gap 主要来自哪里？
14. 当前资本分类是否在外部任务中仍有区分意义？
15. 是否可以称为二阶智能雏形？

最终 verdict 只能选：

- IC3_STRONG_SECOND_ORDER_ALLOCATOR_SUPPORTED
- IC3_WEAK_SECOND_ORDER_SIGNAL_CONFIRMED
- IC3_SYNTH_ONLY_WEAK_SIGNAL
- IC3_FAILS_BEST_SINGLE
- IC3_FEATURE_ENGINEERING_REGRESSION
- IC3_NEGATIVE_TRANSFER_UNPROTECTED
- IC3_EXTERNAL_VALIDATION_FAILED
- IC3_MANIFOLD_CONSTRAINT_NEEDED
- IC3_REPORT_INTERFACE_INSUFFICIENT
- IC3_BENCHMARK_INVALID

十二、总原则

不要为了证明 ICT 正确而改指标。
不要为了让 AEP 赢而削弱 Memory / Prototype。
不要用 env 特征写规则。
不要用新复合指标绕开 regret。
不要忽略外部基准。
不要忽略 3900% 这类计数异常。
不要宣布“强二阶智能”，除非 Allocator 稳定、成本归一化、跨任务、跨 seed、外部验证地超过 BestSingleCapital。

最终目标不是证明某个资本最强，而是验证：

一个只读取资本绩效报表的闭环资本配置器，能否在成本、扰动、不确定性和目标变化下，稳定超过任何单一资本。

这才是 ICT 中“函数的函数 / 二阶智能”的最小工程形态。